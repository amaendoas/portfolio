import styled from "styled-components";

export const Container = styled.div`
  grid-area: "subHeader";
  position: absolute;
  top: 52px;
  right: 0;
  left: 0;
  display: flex;
  align-items: start;
  justify-content: end;
  padding: 1rem 3rem;
  gap: 1rem;
`